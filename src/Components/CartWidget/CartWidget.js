import cart from './assets/carrito.png'

const CartWidget = () => {

    return (
        <div>
            <img src={cart} alt="cart-widget" />
        </div>
    )
}

export default CartWidget